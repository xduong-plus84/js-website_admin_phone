export function Phone(
  name,
  price,
  img,
  screen,
  backCamera,
  frontCamera,
  desc,
  type
) {
  this.name = name;
  this.price = price;
  this.img = img;
  this.screen = screen;
  this.backCamera = backCamera;
  this.frontCamera = frontCamera;
  this.desc = desc;
  this.type = type;
}
